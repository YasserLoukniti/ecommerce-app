import { Image, StyleSheet, Text, View } from 'react-native';

export default function Avatarss() {

    return (
        <View style={styles.footer}>
                    </View>
    );
}

const styles = StyleSheet.create({
    footer: {
        flex: 2,
        backgroundColor: "red",
        alignItems: 'center',
        justifyContent: 'center',
    },
});